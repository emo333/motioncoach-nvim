---@diagnostic disable
local countcompression = require('motioncoach-nvim.suggestions.advanced.countcompression')

describe('suggestions.advanced.advanced.countcompression.lua', function()
  it('exists as a table', function()
    assert.is_table(countcompression)
  end)
end)
